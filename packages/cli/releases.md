# Releases

`@anvia/cli` enters the synchronized Anvia 1.0 release train in the next RC.

| Version | Summary |
| --- | --- |
| Next RC source | Added shadcn-backed `init` and `add` commands for editable app-owned chat components built on `@anvia/react-ui` primitives. |

The first release contains registry items for a complete chat plus independent thread, message,
composer, attachment, Markdown, and fallback-tool components.

Review the [`@anvia/cli` package source](https://github.com/anvia-hq/anvia/tree/staging/packages/cli)
and [changeset](https://github.com/anvia-hq/anvia/blob/staging/.changeset/add-app-owned-react-ui.md).
